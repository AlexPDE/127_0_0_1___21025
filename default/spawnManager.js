"use strict";
let spawnManager;
spawnManager = (room) => {
    const fastFillers = _.filter(Game.creeps, (creep) => creep.memory.role == MemoryRole.FASTFILLER);
    if (Memory.baseManager[room.name].fastFillerActive) {
    }
};
//# sourceMappingURL=spawnManager.js.map